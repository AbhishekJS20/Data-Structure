package AppnaCollege;

public class BasicJavaConcept {
    
    public static void main(String[] args)
    {
        // int x = 9, y = 12;int a = 2, b = 4, c = 6;
        // int exp = 4/3 * (x + 34) + 9 * (a + b * c) + (3 + y * (2 + a)) / (a + b*y);
        // System.out.println(exp);

        // int x = 10, y = 5;
        // int exp1 = (y * (x / y + x / y)); // (5 * (2 + 2)) = 5 * 4 = 20
        // int exp2 = (y * x / y + y * x / y); // (5*10/5 + 5*10/5) = (10 + 10) = 20
        // System.out.println(exp1); // 20
        // System.out.println(exp2); // 20

        // int x, y, z;
        // x = y = z = 2;        // x=2, y=2, z=2
        // x += y;               // x = x + y = 4
        // y -= z;               // y = y - z = 0
        // z /= (x + y);         // z = z / (x + y) = 2 / (4 + 0) = 0 (integer division)
        // System.out.println(x + " " + y + " " + z); // Output: 4 0 0
        
    }
}
